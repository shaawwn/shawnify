import {useState, useEffect} from 'react';

function ErrorView(props) {

    return(
        <div className="error-view">
            <h1>Only users with premium accounts can use App :( But its Spotify is pretty worth it!</h1>
        </div>
    )
}

export default ErrorView