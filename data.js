// need to come up with some kind of standard for object to pass to comps so I can stop having to constantly go back

// track data

const data = {
    // id, album, name, image, artist, duration, progress
    // pass to trackTable (any) in order to play songs
}

const playbackState = {
    // is_playing, progress_ms, shuffle_state, repeat_state
}


